export enum ComplaintStatus {
  PENDING = "PENDING",
  INPROGRESS = "INPROGRESS",
  RESOLVED = "RESOLVED",
  REJECTED = "REJECTED",
}

export enum ReqType {
  BODY = "body",
  PARAMS = "params",
  QUERY = "query",
}
